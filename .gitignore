# Regenerated by tools/generate_*.py -- not committed, so the repo stays small and the
# datasets are reproducible rather than trusted.
data/
truth/
results_recovery_benchmark.json
__pycache__/
*.pyc
*.asv
*.mat
tests/[0-9][0-9]_*.py
